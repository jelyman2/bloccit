<div class="row">
  <div class="col-md-8">
    <div class="media">
      <br />
      // #12
      <% avatar_url = @user.avatar_url(128) %>
      <% if avatar_url %>
          <div class="media-left">
            // #13
            <%= image_tag avatar_url, class: 'media-object' %>
          </div>
      <% end %>
      <div class="media-body">
        <h2 class="media-heading"><%= @user.name %></h2>
        <small>
          <%= pluralize(@user.posts.count, 'post') %>,
          <%= pluralize(@user.comments.count, 'comment') %>
        </small>
      </div>
    </div>
  </div>
</div>

// #14
<h2>Posts</h2>
<% if user_created_posts? %>
  <%= render @user.posts %>
<% else %>
  <em>No posts to display for this user.</em>
<% end %>

<h2>Comments</h2>
<% if user_created_comments? %>
    <%= render @user.comments %>
<% else %>
    <em>No comments to display for this user.</em>
<% end %>