<h4>Add a comment</h4>
<!-- #17 -->
<% if type == :topic %>
    <%= form_for [topic, comment] do |f| %>
      <%= render 'form_fields' %>
    <% end %>
<% else %>
    <%= form_for [post, comment] do |f| %>
        <%= render 'form_fields' %>
    <% end %>
<% end %>